#include <bits/stdc++.h>

using namespace std;

int n, a[505];
int main()
{
    cin >> n;
    for(int i = 0; i < n; i++) cin >> a[i];
    for(int i = 0; i < n; i++)
    {
        int x = a[i];
        int cx = i, cy = i;
    }
}